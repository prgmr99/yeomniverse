name: Daily FinBrief

on:
  # 매일 한국시간 오전 7시 30분 실행 (UTC 22:30 = KST 07:30)
  # GitHub Actions cron은 10~30분 지연이 있어 실제 발송은 ~08:00 KST
  schedule:
    - cron: '30 22 * * *'
  
  # 수동 실행 옵션
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send-daily-briefing:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: finbrief/package-lock.json
      
      - name: Install dependencies
        working-directory: finbrief
        run: npm ci
      
      - name: Run daily briefing
        working-directory: finbrief
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          RESEND_FROM_EMAIL: ${{ secrets.RESEND_FROM_EMAIL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          NEXT_PUBLIC_BASE_URL: ${{ secrets.NEXT_PUBLIC_BASE_URL }}
          USE_PERSONALIZED_EMAILS: 'true'
        run: npm run send-briefing
      
      - name: Copy data to web app
        run: |
          # Get today's date
          TODAY=$(date +%Y-%m-%d)

          # Create directories if not exists
          mkdir -p apps/finbrief/public/data

          # Copy today's public briefing to latest.json and date-stamped file
          if [ -f "finbrief/data/public/$TODAY.json" ]; then
            cp "finbrief/data/public/$TODAY.json" apps/finbrief/public/data/latest.json
            cp "finbrief/data/public/$TODAY.json" "apps/finbrief/public/data/$TODAY.json"
            echo "✅ Copied public JSON to apps/finbrief/public/data/latest.json and $TODAY.json"
          else
            echo "⚠️ No public briefing file found for $TODAY"
          fi

      - name: Commit and push data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add finbrief/data/ apps/finbrief/public/data/
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: update daily briefing data $(date +'%Y-%m-%d') [skip ci]"
          git push
